let n;
do{
  n = prompt("Введіть число N",);
    n = parseInt(n,10);
} while(isNaN(n));
console.log (n, typeof(n));

let m;
do{
  m = prompt("Введіть число M",);
    m = parseInt(m,10);
} while(isNaN(m));
console.log (m,typeof(m));


